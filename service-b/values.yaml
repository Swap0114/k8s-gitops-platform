replicaCount: 2

image:
  repository: service-b
  tag: "1.0.0"
  pullPolicy: Never

service:
  type: ClusterIP
  port: 3001

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
